/*
JavaScriptをもちいて、以下の体重と身長のBMI（体格指数）を出力できるプログラムを記述する課題です。

体重：68kg
身長：1.7m
計算式：[体重(kg)]÷[身長(m)×身長(m)]
+	左の値と右の値を足す。	45 + 18（63）
-	左の値から右の値を引く。	30 - 12（18）
*	左の値と右の値を掛ける。	15 * 6（90）
/	左の値を右の値で割る。	30 / 3（10）
%	左の値を右の値で割った余りを求める。	45 % 8（5）

*/

let kg=68;
let m =1.7;
let ans;

ans=kg/(m*m);



console.log(ans);
